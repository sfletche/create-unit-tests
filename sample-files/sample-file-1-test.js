import SampleFile1 from './sample-files/sample-file-1';

describe('SampleFile1', () => {

  describe('getFormattedCurrency', () => {
    it('needs a unit test', () => {
      expect(true).toEqual(false);
    });
  });

  describe('formatDate', () => {
    it('needs a unit test', () => {
      expect(true).toEqual(false);
    });
  });

  describe('getFormattedPercent', () => {
    it('needs a unit test', () => {
      expect(true).toEqual(false);
    });
  });

  describe('toDate', () => {
    it('needs a unit test', () => {
      expect(true).toEqual(false);
    });
  });

  describe('isValidDate', () => {
    it('needs a unit test', () => {
      expect(true).toEqual(false);
    });
  });
});
